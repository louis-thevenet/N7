LOADL 5
LOADL 10
;Load variable a from offset 0
;Building a couple < a, b>
LOAD (1) 0[SB]
;Load variable b from offset 1
LOAD (1) 1[SB]
;Building a couple < 20, 42>
LOADL 20
LOADL 42
;Load variable a from offset 0
;(a + b)
;Building a couple < (a + b), (b - a)>
LOAD (1) 0[SB]
;Load variable b from offset 1
LOAD (1) 1[SB]
SUBR IAdd
;Load variable b from offset 1
;(b - a)
LOAD (1) 1[SB]
;Load variable a from offset 0
LOAD (1) 0[SB]
SUBR ISub
;Building a couple < true, 42>
LOADL 1
LOADL 42
;Load variable couple4 from offset 8
;Loading the first element of Couple(fstcouple4)
;Print:
;(fstcouple4)
LOADL 8
LOADI (1)
SUBR BOut
;Load variable couple4 from offset 8
;Loading the second element of Couple(sndcouple4)
;Print:
;(sndcouple4)
LOADL 8
LOADL 1
SUBR IAdd
LOADI (1)
SUBR IOut
;Load variable couple1 from offset 2
;Loading the first element of Couple(fstcouple1)
LOADL 2
LOADI (1)
;Load variable couple1 from offset 2
;Loading the second element of Couple(sndcouple1)
LOADL 2
LOADL 1
SUBR IAdd
LOADI (1)
;Load variable premier from offset 10
;Print:
;premier
LOAD (1) 10[SB]
SUBR IOut
;Load variable second from offset 11
;Print:
;second
LOAD (1) 11[SB]
SUBR IOut
;Load variable couple1 from offset 2
;Loading the first element of Couple(fstcouple1)
;((fstcouple1) + 1)
;Building a couple < ((fstcouple1) + 1), ((sndcouple1) + 2)>
;Assignement:
; couple1 
;from:
;< ((fstcouple1) + 1), ((sndcouple1) + 2)>
LOADL 2
LOADI (1)
LOADL 1
SUBR IAdd
;Load variable couple1 from offset 2
;Loading the second element of Couple(sndcouple1)
;((sndcouple1) + 2)
LOADL 2
LOADL 1
SUBR IAdd
LOADI (1)
LOADL 2
SUBR IAdd
;Assign couple1 from SB + 2
LOADL 2
STOREI (2)
;Load variable couple1 from offset 2
;Loading the first element of Couple(fstcouple1)
;Print:
;(fstcouple1)
LOADL 2
LOADI (1)
SUBR IOut
;Load variable couple1 from offset 2
;Loading the second element of Couple(sndcouple1)
;Print:
;(sndcouple1)
LOADL 2
LOADL 1
SUBR IAdd
LOADI (1)
SUBR IOut
;Load variable couple2 from offset 4
;Loading the first element of Couple(fstcouple2)
;((fstcouple2) + 2)
;Building a couple < ((fstcouple2) + 2), ((sndcouple2) + 2)>
;Assignement:
; couple2 
;from:
;< ((fstcouple2) + 2), ((sndcouple2) + 2)>
LOADL 4
LOADI (1)
LOADL 2
SUBR IAdd
;Load variable couple2 from offset 4
;Loading the second element of Couple(sndcouple2)
;((sndcouple2) + 2)
LOADL 4
LOADL 1
SUBR IAdd
LOADI (1)
LOADL 2
SUBR IAdd
;Assign couple2 from SB + 4
LOADL 4
STOREI (2)
;Load variable couple2 from offset 4
;Loading the first element of Couple(fstcouple2)
;Print:
;(fstcouple2)
LOADL 4
LOADI (1)
SUBR IOut
;Load variable couple2 from offset 4
;Loading the second element of Couple(sndcouple2)
;Print:
;(sndcouple2)
LOADL 4
LOADL 1
SUBR IAdd
LOADI (1)
SUBR IOut
HALT

